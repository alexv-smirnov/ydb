name: Learn
on:
  workflow_dispatch:
    inputs:
      str1:
        type: string
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
    - name: Check out
      uses: actions/checkout@v4
    - name: Step2
      uses: ./.github/actions/learn
      with:
        str1: ${{ inputs.str1 }}
